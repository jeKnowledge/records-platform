Router.map(function () {
  this.route('home', { path: '/' });
  this.route('admin', { path: '/admin' });
  this.route('meetings', { path: '/meetings' });
  this.route('atas', { path: '/atas' });  
});
